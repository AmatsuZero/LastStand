# Android / Shared 测试任务枚举（只读）

- 时间：2026-09-14（Asia/Shanghai）；Android `5543c83e`，Shared `4f36969b`。
- 均使用 Java 21、Android SDK，以及 `-PugreenHomeSharedPath=/private/tmp/ugreen-android-architecture-20260914/shared-release-1.7.0-9q2u2wrq -PfirebaseCrashlyticsMappingUpload=false --no-daemon --console=plain`。
- 实际执行的仅是任务报告：Android `:app:tasks --all`（0，20 秒）、Android `tasks --all`（0，18 秒）、Shared standalone `tasks --all`（0，21 秒）。未执行测试、构建、安装或依赖下载。

## 当前可调用任务

| 范围 | 枚举到的相关任务 | 结论边界 |
|---|---|---|
| App instrumentation | `:app:connectedAndroidTest`、`:app:connectedCheck`、`:app:connectedDomesticDebugAndroidTest`、`:app:connectedOverseasDebugAndroidTest`、`:app:deviceAndroidTest` | 任务当前注册；不表示设备可用或任务已执行。 |
| Android 全项目 instrumentation | `tasks --all` 枚举到 app 与 base、bluetooth、camera、net、repository、rn-platform 等模块的 `connectedAndroidTest` / `connectedDebugAndroidTest` / `connectedCheck`，以及 `player-core:androidDeviceCheck` | 完整清单见 root 原始日志；只说明该 Gradle invocation 可见任务。 |
| Shared Android device test | `core-database`、`core-net` 的 `connectedAndroidDeviceTest`、`deviceAndroidTest`；多 Shared 模块的 `androidDeviceCheck` | Shared standalone 枚举结果；未连接设备、未执行。 |
| Shared Android host test | `airtc-appsdk`、`core-database`、`core-kv`、`core-logger`、`core-net`、`core-secure-package`、`core-ui`、`core-util`、`device-thing-model`、`domain-*`、`feature-*`、`sample`、`ugreen-strings-kmp` 的 `testAndroidHostTest`；亦有对应 compile/assemble task | Shared 日志也警告某些 `commonTest` source directory 未启用 host test；不能据此推断所有模块均有 host-test 配置或测试用例。 |
| Coverage | 在三份 `tasks --all` 输出中未枚举到任务名以 `jacoco` / `kover` 开头，或包含 `coverage` 的任务 | 只是在当前快照和本次任务报告中未见这些命名任务；不等同未配置 coverage、没有报告，或覆盖率为 0%。 |

原始完整输出：`测试任务枚举-app-tasks-all.log`、`测试任务枚举-root-tasks-all.log`、`测试任务枚举-shared-tasks-all.log`；命令与退出码汇总见 `测试任务枚举-run-summary.txt`。
