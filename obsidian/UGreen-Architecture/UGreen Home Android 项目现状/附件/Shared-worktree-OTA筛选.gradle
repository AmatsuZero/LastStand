allprojects {
    tasks.withType(org.gradle.api.tasks.testing.Test).configureEach {
        timeout = java.time.Duration.ofSeconds(90)
        if (project.name == 'feature-ota' && name == 'testAndroidHostTest') {
            filter { excludeTestsMatching '*OtaFeatureApiTest' }
        }
    }
}
