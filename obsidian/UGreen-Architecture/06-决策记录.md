# 决策记录（Brainstorming）

## 交付物
App 级混合架构「宪法」：Native / KMP-CMP / RN / H5 职责 + 导航与状态所有权

## 已确认
| 项 | 选择 |
|----|------|
| 时间视图 | **C** 目标态 + 阶段一双视图 |
| 运行时范围 | **C** 三栈为主；H5 为动态面子节（轻量约束） |
| 状态与导航 | **C** 阶段一 Host 编排 → 目标态 KMP 域 SSOT |
| RN/H5 碰设备 | **A** 禁止直连 SDK；只经 Host Bridge → 域 |
| KMP UI | **C** 阶段一可域先共享；目标态新 Feature 默认 CMP |

## 派生红线（待写进 Spec）
- KMP 不做业务热发；动态面 = RN（主）+ H5（运营）
- 新 KMP Feature 对齐 Toolkit Role Contract；Host 拓扑另章
- iOS 交付默认单一 `UgreenHomeShared` XCFramework

## 宪法写法
**方案 1 — Runtime 编排宪法**（已确认）
